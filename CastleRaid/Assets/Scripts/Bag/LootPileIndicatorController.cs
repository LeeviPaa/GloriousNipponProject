using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class LootPileIndicatorController : MonoBehaviour
{
	// Get all child lootables, subscribe to their on grabbed list
	// When all child lootables are removed, stop glitter

	// On lootable script, toggle individual glitter on if removed from pile
}
